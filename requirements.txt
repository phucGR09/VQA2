# ReflectiVA Vietnamese VQA base environment
# Target server CUDA: 12.4
#
# Recommended Python: 3.10
#
# Install:
#   pip install -r requirements.txt
#   pip install flash-attn==2.7.4.post1 --no-build-isolation
#   pip install -r requirements_vn.txt
#
# PyTorch CUDA 12.4 wheels.
--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.6.0+cu124
torchvision==0.21.0+cu124

# Keep this Transformers line pinned. ReflectiVA's custom LLaVA classes are
# written against the 4.x API; upgrading to newer major versions is risky.
transformers==4.43.1
tokenizers==0.19.1

accelerate==0.33.0
deepspeed==0.16.4
peft==0.12.0

# Install flash-attn separately with --no-build-isolation, as shown above.
# Keeping it commented here avoids common pip build-order failures.
# flash-attn==2.7.4.post1

numpy==1.26.4
pillow==10.4.0
tqdm==4.66.5
ujson==5.10.0
requests==2.32.3
packaging==24.1
huggingface-hub==0.24.6
safetensors==0.4.4
sentencepiece==0.2.0
einops==0.8.0
timm==1.0.8
faiss-cpu==1.8.0.post1

# Needed by optional/original ReflectiVA evaluation paths, and harmless for VN.
webdataset==0.2.100
braceexpand==0.1.7
spacy==3.7.5

# Server/demo/eval helpers used by legacy LLaVA scripts.
gradio==4.44.0
fastapi==0.112.2
uvicorn==0.30.6
shortuuid==1.0.13
pandas==2.2.2
openai==1.42.0
editdistance==0.8.1
